CREATE OR REPLACE PROCEDURE P_AUMENTAR_SALARIOS (PORCENTAJE IN NUMBER) IS
BEGIN
    UPDATE B_CATEGORIAS_SALARIALES
    SET ASIGNACION = ASIGNACION * (1 + (PORCENTAJE / 100))
    WHERE FECHA_FIN IS NULL;
END;
/
